spring.application.name=vehicle-api
# MongoDB connection string secured via environment variable to avoid plain-text credentials.
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=vehicle-db
